    # variables and expressions and statements
name = 'Beau'; print(name) # this an inline comment
age  = 39
    # comments
# you cant use numbers, exclamtion marks or bascally an other symbols as variables 
# if = 'hi' ==> you can not use this as a variable because it is a keyword
# an example of keywords are (for, if, while, import)

    # illustrations of data types 
print(type(name) == str)
print(isinstance(name, str))

age = float(2)
print(isinstance(age, int))
print(isinstance(age, float))

age1 = int('20')
print(isinstance(age1, int))
print(isinstance(age1, str))

# can not convert a word into an integer though
